# ==========================================================================
# hugo.toml — Industrial Manufacturer Template (Multilingual Configuration)
# ==========================================================================
# >>> EDIT baseURL (and optionally title) for your site. <<<
# Base content language: English (en) at site root.
# Secondary languages served under language prefixes: /de/ /ja/ /fr/ /es/
# All other branding lives in config/_default/params.toml.
# ==========================================================================

baseURL = "https://diecasting.github.io/Woodsat.github.io/"
locale = "en-US"
title = "Woodsat"
defaultContentLanguage = "en"
defaultContentLanguageInSubdir = false
enableRobotsTXT = true
enableGitInfo = true
enableMissingTranslationPlaceholders = false

# baseURL contains a sub-path (GitHub project page). Root-relative links written
# in markdown (e.g. /de/contact/) must be rewritten to include that sub-path,
# otherwise every in-content link 404s. canonifyURLs handles this globally.
canonifyURLs = true

# Pagination
paginate = 12

# Output formats — HTML + RSS (home/section) + HTML (page).
# Hugo generates /sitemap.xml (all languages) automatically.
[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
  page = ["HTML"]

# Taxonomies
[taxonomies]
  category = "categories"
  tag = "tags"

# Minify configuration
[minify]
  minifyOutput = true
  [minify.tdewolff]
    [minify.tdewolff.html]
      keepComments = false
    [minify.tdewolff.css]
      precision = 0
    [minify.tdewolff.js]
      precision = 0

# Markup configuration
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.parser]
      autoHeadingID = true
      autoHeadingIDType = "github"
  [markup.highlight]
    style = "github"
    lineNos = false
    noClasses = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4

# RSS service limits
[services]
  [services.rss]
    limit = 10
